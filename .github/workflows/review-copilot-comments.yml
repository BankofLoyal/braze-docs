# Evaluates Copilot review comments on auto-translation PRs using Claude.
# Applies valid fixes, updates glossaries, and posts a summary.
#
# Triggered when Copilot submits a review on a PR with the 'auto-translation' label.
# After evaluation, swaps "In Review" → "status: done" to signal human readiness.
name: Review Copilot comments

on:
  pull_request_review:
    types: [submitted]

permissions:
  contents: write
  pull-requests: write

jobs:
  review-copilot:
    if: >-
      (github.event.review.user.login == 'copilot[bot]' ||
       github.event.review.user.login == 'github-copilot[bot]') &&
      contains(github.event.pull_request.labels.*.name, 'auto-translation') &&
      github.event.pull_request.user.login == 'github-actions[bot]' &&
      startsWith(github.event.pull_request.head.ref, 'auto-translate/')
    runs-on: ubuntu-latest
    concurrency:
      group: copilot-review-pr-${{ github.event.pull_request.number }}
      cancel-in-progress: false
    env:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install anthropic

      - name: Evaluate Copilot comments
        id: review
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/review_copilot.py

      - name: Commit and push fixes
        if: steps.review.outputs.fixes_applied != '0'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add _lang/ scripts/glossaries/
          git diff --cached --quiet && echo "No changes to commit" && exit 0
          git commit -m "fix: apply Copilot review suggestions (automated)"
          git push

      - name: Post summary comment
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -f copilot_review_summary.md ]; then
            gh pr comment ${{ github.event.pull_request.number }} \
              --body-file copilot_review_summary.md
          fi

      - name: Update labels
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr edit ${{ github.event.pull_request.number }} \
            --remove-label "In Review" \
            --add-label "status: done"
